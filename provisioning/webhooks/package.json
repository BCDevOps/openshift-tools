{
  "name": "webhooks",
  "version": "1.0.0",
  "description": "Webhook to work with provisioning tools such as OSBA",
  "main": "validator.js",
  "scripts": {
    "start": "ts-node validator.ts",
    "format": "prettier --write \"**/*.{ts,js,md,json}\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fast-json-patch": "^3.1.1",
    "fastify": "^1.10.0",
    "immutable": "^4.0.0-rc.9"
  },
  "devDependencies": {
    "prettier": "^1.14.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1"
  }
}
